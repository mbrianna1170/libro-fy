<article class="post">
  <div class="title">
    <a href="{{book.book_url}}" target="_blank">{{book.book_name}}</a>
    <img class="book-img" src="{{book.img_url}}" alt="{{book.book_name}}" />
    <span>Written by: {{book.author_name}}</span>
  </div>
  <div class="meta">
    {{book.vote_count}} point(s) by you {{user.username}} 
    |
    <a href="/book/{{book.id}}">{{book.comments.length}} comment(s)</a>
  </div>
</article>

{{#if loggedIn}}
<form class="comment-form">
  <div>
    <textarea name="comment-body"></textarea>
  </div>

  <div>
    <button type="submit">add comment</button>
    <button type="button" class="upvote-btn">upvote</button>
  </div>
</form>
{{/if}}

<div class="comments">
  {{#each book.comments}}
  <section class="comment">
    <div class="meta">
      {{user.username}} 
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
<script src="/javascript/upvote.js"></script>
{{/if}}